{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "assemblyscript": "^0.27.23"
  },
  "scripts": {
    "asbuild": "asc assembly/index.ts --target release --importMemory --runtime stub -Osize --converge --pedantic",
    "asbuild:debug": "asc assembly/index.ts --target debug --importMemory"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
